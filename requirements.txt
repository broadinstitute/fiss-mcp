# Terra.Bio MCP Server Dependencies
# =================================
# This file is kept for backward compatibility and quick setup.
# The authoritative source of dependencies is pyproject.toml.

# Build dependencies (must be installed first)
# Required for firecloud installation - see broadinstitute/fiss#192
setuptools<80

# Install package with dev dependencies from pyproject.toml
# Use --no-build-isolation for firecloud compatibility
-e .[dev]
